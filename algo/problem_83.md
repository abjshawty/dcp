# Problem 83 — Invert Binary Tree

**Difficulty:** Easy

## Problem

Given a binary tree, invert it (mirror it) in place.

## Approach — Recursive Swap

Recursively invert the left and right subtrees. Then swap them at the current node.

**Time:** O(N)  
**Space:** O(H) where H is tree height
