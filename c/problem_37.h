#ifndef PROBLEM_37_H
#define PROBLEM_37_H

/* powerSet fills subsets[][64] and returns count (2^n). */
int  powerSet(const int *nums, int n, int subsets[][64], int *subsetSizes);
void problem37(void);

#endif
