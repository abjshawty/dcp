#ifndef PROBLEM_42_H
#define PROBLEM_42_H

/* subsetSum fills result[] and returns count, or -1 if no subset found. */
int  subsetSum42(const int *nums, int n, int target, int *result);
void problem42(void);

#endif
